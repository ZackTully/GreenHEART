
stop_index: 125
options:
  tqdm_progress: true
  verbose: true
  save_sysid: false
system:
  system_graph_config: !include ../system_graph_config.yaml
component_config: !include ../subsystem_config.yaml
forecast:
  horizon: 3
  method: perfect_persistence_interp
  method_config:
    perfect_fraction: 0
    
dispatch:
  update_period: 1
  simple_system: 
